# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# ローカル設定（アカウント固有の ARN やプロファイル名を含む）
.env

# 推論結果キャッシュ
.cache/

# pytest
.pytest_cache/
.ruff_cache/

# 記事本文（note.com に投稿する原稿）と作業用ドキュメント。リポジトリには含めない。
articles/
HANDOVER.md
HISTORY.md
PLAN.md

# 効果検証の実測結果（Bedrock を実際に呼んだ記録。再取得には課金と30分かかる）
# .cache は推論キャッシュを捨てるために rm -rf することがあるので、別ディレクトリに置く。
eval-results/

# 比較実験用のコーパス（環境省の通知と各施設の公表ページの本文）。
# 第三者の著作物なので再配布はしない。uv run onsen corpus build で組み直せる。
corpus/

# onsen graph の出力（図はリポジトリに置かない）
*.png
*.svg
*.dot
